<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pulsometer" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="21" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.samsung.android.providers.context.permission.WRITE_USE_APP_FEATURE_SURVEY" />
    <!--
     To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:name=".Logic.SHealth.PluginTrackerProvider"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>

        <!-- Registration Service to handle intent from SHealth by click action -->
        <service
            android:name=".Logic.SHealth.MyTrackerService"
            android:exported="true" />
        <!-- Registration PluginService with Tracker Information -->
        <service
            android:name="com.samsung.android.sdk.shealth.PluginService"
            android:exported="true" >
            <meta-data
                android:name="tracker.test"
                android:value="@string/testtracker_manifest" />
        </service>

        <activity
            android:name=".LoginSuccessfullActivity"
            android:label="@string/title_activity_login_suuccessfull" >
        </activity>
        <activity
            android:name=".CaseActivity"
            android:label="@string/title_activity_case" >
        </activity>
        <activity
            android:name=".AnalisysActivity"
            android:label="@string/title_activity_analisys" >
        </activity>
    </application>

</manifest>
